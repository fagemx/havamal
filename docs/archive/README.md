# Archive

Retired documents. Two families, two reasons — both kept for provenance, none current method.

**Superseded by software ([edda](https://github.com/fagemx/edda)):** `execution-control`, `control-plane` (+ json/schema), `lane-parallelism`, `status-command`, `execution-state`. These taught hand-maintaining execution state and control planes in markdown. That job now belongs to running software: edda carries facts, decisions, and coordination automatically. Havamal keeps only the judgment layer.

**Out of scope:** `strong-session-protocol`, `strong-session-bootstrap`, `orchestrator-session-bootstrap`, `agent-role-distinction`, `triad-development`. Session orchestration drifted in while the surrounding toolchain was immature. Havamal's test for every document: *does it teach extracting judgment, or managing state?* State management lives elsewhere.

The retirement rule applied here is havamal's own maintenance doctrine: **doctrine should get smaller and sharper over time, not bigger and heavier.**
